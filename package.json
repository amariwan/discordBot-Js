{
  "name": "dashcord",
  "version": "0.0.1",
  "description": "A simple example of using Discord's OAuth",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "discord.js": "^14.7.1"
  }
}
